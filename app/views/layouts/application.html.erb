<!DOCTYPE html>
<html>
  <head>
    <title>KEYPON</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=M+PLUS+Rounded+1c" rel="stylesheet">
  </head>
  <body>
    <%# HEADER %>
    <header class="sticky-top">
      <nav class="navbar navbar-expand navbar-dark bg-dark">
        <%# LOGO %>
        <%= link_to search_keymaps_path do %>
          <div class="logo_area d-flex align-items-center">
            <div class="logo mx-auto">
            </div>
          </div>
        <% end %>
        <%# COLLAPSE BUTTON %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <%# L MENU %>
        <div class="collapse navbar-collapse" id="navbarColor02">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="#">Left1 <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Left2</a>
            </li>
          </ul>
          <%# R MENU %>
          <% if user_signed_in? %>
            <ul class="navbar-nav">
              <li class="nav-item active">
                <a class="nav-link" href="#">Right1 <span class="sr-only">(current)</span></a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">＋</a>
              </li>
            </ul>
            <ul class="navbar-nav">
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                  <%= image_tag current_user.image[:small].url, class: "border border-dark rounded user-image-sm", fallback: "no_image.jpg" %>
                </a>
                <div class="dropdown-menu dropdown-menu-right" style="">
                  <a class="dropdown-item" href="#">My Page</a>
                  <%= link_to "User Settings", settings_users_path, class: "dropdown-item" %>
                  <div class="dropdown-divider"></div>
                  <%= link_to "Sign out", destroy_user_session_path, class: "dropdown-item", method: :delete %>
                </div>
              </li>
            </ul>
          <% else %>
            <ul class="navbar-nav mr-2">
              <li><%= link_to "Sign-up", new_user_registration_path, class: "nav-link" %></li>
              <li><%= link_to "Log-in", new_user_session_path, class: "nav-link" %></li>
            </ul>
          <% end %>
        </div>
      </nav>
    </header>
    <%# MAIN %>
    <main>
      <div class="container">
        <%= yield %>
      </div>
    </main>
    <%# FOOTER %>
    <footer>
    </footer>
  </body>
</html>
